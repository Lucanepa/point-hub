<?xml version="1.0" encoding="UTF-8"?>
<lint>
    <!-- targetSdk 35 is deliberate: API 36 ignores screenOrientation on tablets (sw >= 600dp),
         and this app must stay landscape. Revisit on purpose, not because lint says so. -->
    <issue id="OldTargetApi" severity="ignore" />
    <!-- Dependency version nags need network and are not a build gate. -->
    <issue id="GradleDependency" severity="ignore" />
    <issue id="NewerVersionAvailable" severity="ignore" />
    <issue id="AndroidGradlePluginVersion" severity="ignore" />
    <!-- An English-only kiosk app for one club; admin/status text is built in code on purpose. -->
    <issue id="SetTextI18n" severity="ignore" />
</lint>
