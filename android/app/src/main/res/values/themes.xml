<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Kiosk host: no action bar, fullscreen, black behind the WebView. -->
    <style name="Theme.PointHub" parent="android:Theme.Material.NoActionBar.Fullscreen">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:colorAccent">@color/gold</item>
        <item name="android:colorPrimary">@color/ink</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
    <!-- Native admin / setup screens. -->
    <style name="Theme.PointHub.Admin" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/ink</item>
        <item name="android:colorBackground">@color/ink</item>
        <item name="android:colorAccent">@color/gold</item>
        <item name="android:colorPrimary">@color/ink</item>
        <item name="android:statusBarColor">@color/ink</item>
        <item name="android:navigationBarColor">@color/ink</item>
        <item name="android:textColorPrimary">@color/text</item>
        <item name="android:textColorSecondary">@color/muted</item>
    </style>
    <style name="PH.Button" parent="android:Widget.Material.Button.Colored">
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">18sp</item>
        <item name="android:minHeight">56dp</item>
        <item name="android:textColor">@color/ink</item>
    </style>
    <style name="PH.Button.Plain" parent="android:Widget.Material.Button">
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">18sp</item>
        <item name="android:minHeight">56dp</item>
    </style>
</resources>
